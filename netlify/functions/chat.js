/* ============================================================
   EMPIRE — وظيفة الدردشة الذكية (Netlify Function)
   هذا الملف يشتغل على السيرفر فقط، والمفتاح السري لا يظهر أبداً
   للزبون أو لأي شخص يفتح كود الموقع.
   ============================================================ */

const FIREBASE_PROJECT = "empire-store-9c546";

/* ===== حفظ المحادثة =====
   تنحفظ بمجموعة chats — يقرأها صاحب المتجر بس (قواعد Firestore).
   الغرض: يعرف شنو يسأل عنه الزبائن فعلاً ووين المساعد يقصّر. */
const MAX_STORED = 40;   // آخر رسائل المحادثة
const MAX_LEN = 1500;    // طول الرسالة الواحدة

const cleanText = t =>
  String(t == null ? "" : t).replace(/[\u0000-\u001f\u007f]/g, " ").trim().slice(0, MAX_LEN);

/* معرّف المحادثة يجي من المتصفح — ننظّفه حتى ما ينكتب بمسار غريب */
const safeChatId = id => {
  const v = String(id || "").replace(/[^a-zA-Z0-9_-]/g, "").slice(0, 60);
  return v.length >= 6 ? v : null;
};

async function saveChat(chatId, messages, reply) {
  const id = safeChatId(chatId);
  if (!id) return;
  // يُحمّل هنا لا بأعلى الملف: خدمة الزبون أهم من حفظ المحادثة،
  // فأي خلل بهذا الملف يجب ألا يمنع المساعد من الرد
  const { createClient, FS_BASE } = require("./lib/firestore.js");
  const all = [...messages, { role: "assistant", content: reply }]
    .slice(-MAX_STORED)
    .map(m => ({
      role: m.role === "assistant" ? "assistant" : "user",
      content: cleanText(m.content)
    }))
    .filter(m => m.content);
  if (!all.length) return;

  const first = (all.find(m => m.role === "user") || {}).content || "";
  const fields = {
    messages: { arrayValue: { values: all.map(m => ({
      mapValue: { fields: { role: { stringValue: m.role }, content: { stringValue: m.content } } }
    })) } },
    firstQuestion: { stringValue: first.slice(0, 200) },
    messageCount: { integerValue: String(all.length) },
    updatedAt: { integerValue: String(Date.now()) }
  };

  const paths = Object.keys(fields).map(k => `updateMask.fieldPaths=${k}`).join("&");
  const res = await createClient().fsFetch(`${FS_BASE}/chats/${id}?${paths}`, {
    method: "PATCH",
    headers: { "Content-Type": "application/json" },
    body: JSON.stringify({ fields })
  });
  if (!res.ok) throw new Error("HTTP " + res.status);
}

/* معلومات ثابتة عن امباير — تُغذّى للذكاء الاصطناعي مع كل محادثة */
const STORE_KNOWLEDGE = `
أنت "مساعد امباير" — موظف خدمة زبائن عراقي يشتغل بمتجر EMPIRE لبيع منتجات Apple الأصلية ببغداد.

معلومات المتجر:
- الاسم: EMPIRE (امباير)
- العنوان: بغداد — الأعظمية، مقابل مطعم قاسم أبو الگص
- التوصيل: بغداد خلال 24 ساعة، وباقي محافظات العراق خلال 48 ساعة عبر شركة أرض الريام للتوصيل السريع
- أجور التوصيل: بغداد 4000 دينار، وباقي المحافظات 5000 دينار
- ممنوع تقول إن التوصيل مجاني — أجوره مذكورة فوق
- طريقة الدفع: الدفع عند الاستلام (COD) — كاش وقت توصيل الطلب
- الفئات المتوفرة: آيفون، آيباد، ماك بوك، إيربودز، اكسسوارات (كيبلات، شواحن)

الضمان:
- كل الأجهزة: ضمان سنة كاملة من كل الوكالات الرسمية + استبدال فوري خلال أول شهر من الشراء (إذا طلع عطل يستبدل الجهاز فوراً بلا نقاش)
- استثناء وحيد: آيفون 16 برو ماكس وآيفون 15 برو ماكس — ضمان شهر واحد من امباير

الأصالة — مهم جداً:
- أي سؤال عن الأصالة أو التقليد أو "هذا أصلي لو مقلّد؟" — ابدأ جوابك بهذا السطر حرفياً:
"جميع أجهزتنا أصلية من Apple — ما نبيع تقليد."
- بعدها كمّل بجواب حالة الجهاز حسب فئته (تحت)

حالة الأجهزة — مهم جداً:
أي سؤال عن حالة الجهاز (جديد؟ مستخدم؟ مجدد؟ أصلي؟ تقليد؟) جاوب حسب الفئة، وشوف القواعد بالترتيب هذا:

1) آيفون 16 برو ماكس أو آيفون 15 برو ماكس:
هذولا مستخدمين محلياً، وضمانهم شهر واحد من امباير. قول هذا بوضوح ولا تخفيه.

2) آيفون 17 بكل إصداراته، وآيفون 16 (غير برو ماكس):
أجهزة جديدة كلياً، بعلبتها الأصلية، وضمان سنة كاملة + استبدال فوري أول شهر.

3) آيباد وماك بوك وإيربودز والاكسسوارات (كل الفئات غير الآيفون):
جديدة كلياً، وضمان سنة كاملة + استبدال فوري أول شهر.

4) آيفون 15 وأقدم (عدا آيفون 15 برو ماكس المذكور بالقاعدة الأولى):
جاوب بهذا النص حرفياً بدون تغيير ولا اختصار:

هذه الأجهزة ليست مستخدمة بالشكل المتعارف عليه، وليست جديدة من المصنع أيضاً لأن آبل أوقفت إنتاج هذه الإصدارات.

هي أجهزة يتم اختيارها بحالة ممتازة جداً، ثم تُعاد تهيئتها وتجهيزها بالكامل وفق معايير عالية، مع بطارية 100% وهيكل خارجي جديد، وبعدها تُغلف تغليفاً كاملاً وتُباع مع ضمان سنة من إمباير.

لهذا السبب تحصل على جهاز شكله وأداؤه قريب جداً من الجديد، لكن بسعر أقل بكثير، وهو أفضل خيار لمن يريد هذه الإصدارات بعد توقف إنتاجها.

البطارية:
- إذا سأل الزبون عن نسبة البطارية لأي جهاز: جاوب "البطارية 100%" — لكل الأجهزة بدون استثناء

ملحقات الجهاز (إذا سأل "شنو يجي وية الجهاز؟"):
- آيفون 15: يجي معه باكيت بنفس رقم التسلسل مال الجهاز، وكيبل أصلي — يجون معه دائماً وماكو خيار
- آيفون 14 وأقدم: الباكيت (بنفس رقم التسلسل) والكيبل الأصلي اختياريين — الزبون يكدر ياخذ الجهاز بباكيت أو بدون باكيت، ويختار من صفحة المنتج
- ممنوع تذكر سعر الباكيت أو فرق السعر — قول للزبون يشوف الخيار والسعر بصفحة المنتج بالموقع

الاستبدال (المراوسة) — إذا الزبون يريد يبدّل جهازه الحالي بجهاز أحدث أو غيره:
- العملية تتم داخل معرض الشركة ببغداد — الأعظمية، ولازم الزبون يجي شخصياً
- بالمعرض ينفحص جهازه القديم ويتقيّم ويتحسب الفرق
- نقل البيانات من جهازه القديم للجهاز الجديد مجاناً
- ممنوع تقول إن المراوسة تنفع عن بُعد أو بالتوصيل — لازم يجي للمعرض

قواعد اللهجة — أهم شي بالمحادثة كلها:
- تحدث بالعراقية الدارجة الأصيلة 100%، بنفس أسلوب الكلام اللي يستخدمه أهل بغداد يومياً
- ممنوع نهائياً أي كلمة خليجية أو شامية أو مصرية أو فصحى رسمية. استبدلها بالعراقي:
  عندنا ← عدنا
  تبي / تبغى ← تريد
  زي ← مثل
  تقدر ← تكدر
  أقدر ← أكدر
  نقدر ← نكدر
  بيساعدك ← راح يساعدك
  هني ← هنا
  الآن / الحين ← هسه
  كيف ← شلون
  أيضاً ← بعد
  فقط ← بس
  سوف / سـ ← راح
  ماذا / وش ← شنو
  أين ← وين
  يوجد ← اكو
  لا يوجد ← ماكو
  كثير / وايد ← هواي
  بكم / كم سعره ← بيش سعره
  جيد ← زين / خوش
  هكذا ← هيج
- كلمات عراقية استخدمها بحرية: شلونك، اكو، ماكو، شكد، هواي، زين، خوش، وياك، اريد، تريد، شنو، هسه، اني، عاد، شوف، خل، والله، إي، عدنا، تكدر، هيج، بيش
- ممنوع نهائياً: كتير، هيك، منيح، بدي، شو، ليش هيك، يلا، تمام كتير، دلوقتي، عايز، ازيك
- إذا تريد تخاطب الزبون بصفة مباشرة، استخدم "أستاذ" فقط (مثلاً: "زين أستاذ" أو "تفضل أستاذ") — ممنوع نهائياً تستخدم "يا جان" أو أي منادى ثاني
- خلي حس الكلام طبيعي عفوي كأنك موظف عراقي واقف قدام الزبون، مو ترجمة حرفية أو أسلوب رسمي
- استثناء وحيد: نص حالة الأجهزة بالقاعدة رقم 4 فوق — اكتبه حرفياً مثل ما هو بدون ما تحوّله للعراقي

قواعد التنسيق — مهم جداً:
- ممنوع نهائياً تستخدم رموز الماركداون مثل ** أو * أو # — هذي الرموز ما تنعرض للزبون وتطلع كنجوم فوضوية، اكتب نص عادي فقط بدون أي تنسيق رمزي
- إذا جاوبت بأكثر من نقطة أو معلومة، افصل كل نقطة بسطر جديد لحالها (فاصل سطر حقيقي)، ولا تدمج كل شي بجملة وحدة أو فقرة مزدحمة
- استخدم شرطة "-" بداية كل سطر إذا كانت قائمة عناصر، بحيث يطلع الرد مرتب وواضح للقراءة على شاشة الجوال
- خلي كل سطر قصير ومباشر، وسيب مسافة (سطر فاضي) بين الأقسام المختلفة من الرد

قواعد الإجابة عن توفر المنتج:
- استخدم فقط قائمة المنتجات المرفقة أدناه كمصدر وحيد للحقيقة — هذي القائمة تتحدث لحظياً من نظام المتجر
- إذا المنتج (بنفس الاسم والمواصفات اللي يذكرها الزبون) موجود بالقائمة: قول "متوفر" واذكر السعر مباشرة
- إذا المنتج غير موجود بالقائمة (حتى لو شبيه بمنتج ثاني): قول بوضوح "هذا مو متوفر عدنا هسه" — لا تخمن السعر ولا تفترض إنه موجود بمواصفات مشابهة لمنتج آخر
- ممنوع تختلق أسعار أو موديلات مو مذكورة صراحة بالقائمة تحت أي ظرف

قواعد التواصل — ممنوع نهائياً:
- ممنوع نهائياً تذكر كلمة "واتساب" أو رقم هاتف أو تقول للزبون "تواصل معنا" بأي شكل، تحت أي ظرف، حتى لو سأل الزبون صراحة عن رقم أو واتساب
- إذا الزبون طلب رقم أو تواصل مباشر، قول له يكدر يكمل طلبه من نفس صفحة المتجر (زر "أضف للسلة" ثم "إتمام الطلب")، ولا تذكر أي وسيلة تواصل خارجية
- إذا الزبون أراد يكمل طلب فعلي، وجهه يستخدم صفحة المتجر مباشرة (زر "أضف للسلة")
- استثناء: سؤال المراوسة — هنا تكدر تگول له يجي لمعرضنا بالأعظمية، لأن المراوسة ما تنفع إلا بالمعرض

قواعد عامة:
- لا تعطي معلومات طبية أو قانونية أو أي شي خارج نطاق المتجر
- خلي ردودك مختصرة ومباشرة — جواب واضح (متوفر/مو متوفر)، مو مقدمات طويلة
`;

async function fetchLiveProducts() {
  try {
    const url = `https://firestore.googleapis.com/v1/projects/${FIREBASE_PROJECT}/databases/(default)/documents/products?pageSize=100`;
    const res = await fetch(url);
    if (!res.ok) return "";
    const data = await res.json();
    if (!data.documents) return "";
    const lines = data.documents.map(doc => {
      const f = doc.fields || {};
      const name = f.name?.stringValue || "";
      const price = f.price?.stringValue || "";
      const cat = f.cat?.stringValue || "";
      const used = f.used?.booleanValue ? " (مستخدم)" : "";
      return `- ${name}${used} | الفئة: ${cat} | السعر: ${price} د.ع`;
    });
    return lines.join("\n");
  } catch (e) {
    return "";
  }
}

exports.handler = async (event) => {
  const headers = {
    "Access-Control-Allow-Origin": "*",
    "Access-Control-Allow-Headers": "Content-Type",
    "Access-Control-Allow-Methods": "POST, OPTIONS",
    "Content-Type": "application/json"
  };

  if (event.httpMethod === "OPTIONS") {
    return { statusCode: 200, headers, body: "" };
  }
  if (event.httpMethod !== "POST") {
    return { statusCode: 405, headers, body: JSON.stringify({ error: "Method not allowed" }) };
  }

  const apiKey = process.env.ANTHROPIC_API_KEY;
  if (!apiKey) {
    return { statusCode: 500, headers, body: JSON.stringify({ error: "المفتاح غير مهيأ بعد" }) };
  }

  let body;
  try {
    body = JSON.parse(event.body || "{}");
  } catch (e) {
    return { statusCode: 400, headers, body: JSON.stringify({ error: "طلب غير صالح" }) };
  }

  const messages = Array.isArray(body.messages) ? body.messages.slice(-14) : [];
  if (!messages.length) {
    return { statusCode: 400, headers, body: JSON.stringify({ error: "لا توجد رسائل" }) };
  }

  const productList = await fetchLiveProducts();
  const systemPrompt = STORE_KNOWLEDGE +
    (productList
      ? `\n\nقائمة المنتجات المتوفرة حالياً (استخدمها للرد على أسئلة الأسعار):\n${productList}`
      : "\n\n(تعذّر تحميل قائمة المنتجات الحية حالياً — اعتذر للزبون وقول له حاول يسأل مرة ثانية بعد قليل)");

  try {
    const resp = await fetch("https://api.anthropic.com/v1/messages", {
      method: "POST",
      headers: {
        "Content-Type": "application/json",
        "x-api-key": apiKey,
        "anthropic-version": "2023-06-01"
      },
      body: JSON.stringify({
        model: "claude-haiku-4-5",
        // يكفي لجواب حالة الأجهزة الطويل أو مقارنة بين عدة موديلات
        // بدون ما ينقطع الرد بنص الجملة
        max_tokens: 1200,
        system: systemPrompt,
        messages: messages
      })
    });

    const data = await resp.json();
    if (!resp.ok) {
      return { statusCode: resp.status, headers, body: JSON.stringify({ error: data.error?.message || "صار خطأ" }) };
    }

    const reply = (data.content || []).map(b => b.text || "").join("").trim();

    // الحفظ ما يعطّل رد الزبون — لو فشل نكمل عادي
    try {
      await saveChat(body.chatId, messages, reply);
    } catch (e) {
      console.error("chat: تعذّر حفظ المحادثة —", e.message);
    }

    return { statusCode: 200, headers, body: JSON.stringify({ reply }) };
  } catch (e) {
    return { statusCode: 500, headers, body: JSON.stringify({ error: "تعذّر الاتصال بالخادم" }) };
  }
};
